import React from "react";
import { HeaderContainer, Title } from "../styles/header.module";

const Header = () => {
  return (
    <HeaderContainer>
      <Title>하우키키</Title>
    </HeaderContainer>
  );
};

export default Header;
